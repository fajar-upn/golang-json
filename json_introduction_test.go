package main

/**
 * Intro JSON
 * JSON acronym from Javascript Object Notation, which is
 * a structured format data which forms like an Object in Javascript
 *
 * JSON is a structure format data which mostly used, when we create
 * RESTful API
 *
 * for more information about JSON:
 * https://www.json.org/json-en.html
 */

/**
 * Package JSON
 * Golang already has package json, where we can use this package for do
 * conversion data to JSON (encode) or otherwise (decode)
 *
 * for more information about package JSON:
 * https://pkg.go.dev/encoding/json
 */
